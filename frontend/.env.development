# Loaded by Vite when running `npm run dev`.
# Vite's proxy in vite.config.ts forwards /api → http://localhost:5000,
# so the empty/relative VITE_API_BASE works against the local Flask backend.
VITE_API_BASE=/api
VITE_GOOGLE_CLIENT_ID=21187440512-7h8lpgfs8ttk4efti67s1otjj57ufoi2.apps.googleusercontent.com
