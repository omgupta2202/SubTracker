/**
 * SubTracker module — public surface.
 *
 * The host app imports `Dashboard` from here. Internals (cards, hooks,
 * types, API client) are implementation details and may move within
 * the module without breaking callers.
 */
export { Dashboard } from "./components/Dashboard";
