# Copy to .env.local and edit. Read by Expo at `npx expo start`.
# Replace with your laptop's LAN IP (run `ipconfig getifaddr en0` on macOS).
# Phones cannot reach localhost on your laptop — they need the IP.
EXPO_PUBLIC_API_BASE=http://192.168.1.8:5000/api
